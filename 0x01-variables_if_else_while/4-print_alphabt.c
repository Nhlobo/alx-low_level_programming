#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int lowerCase - 'a';
while (lowerCase <= 'z')
{
lowerCase += 1;
}
else
{
putchar(lowerCase);
lowerCase += 1;
}
{
putchar('\n');
return (0);
}
